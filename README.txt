SPARK TRANSPORTATION v0.3 UPDATE

New: logo, regional driver profiles, effective dates, separate rates, per-entry overrides, rate snapshots, backup and restore.

To update GitHub Pages: export a backup first, unzip this package, replace index.html, manifest.json, sw.js, icon-192.png and icon-512.png in the same repository, then commit. Close and reopen the iPhone app after GitHub Pages deploys.
